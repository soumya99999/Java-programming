import java.util.Scanner;

public class StringFunctions {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input including space
        String str = sc.nextLine();
        // Input excluding space
        String str1 = sc.next();
        // trim() -> To remove white space from left and right
        // toUpperCase() & toLowerCase()
        // substring(int start,int end)/ substring(int start) , charAt(int pos)
        // .toCharArray()
        // str1.compareTo(str2) -> result will be positive , negative or 0
        //
    }
}
